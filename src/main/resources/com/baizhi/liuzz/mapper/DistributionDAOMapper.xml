<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.baizhi.liuzz.dao.DistributionDAO">
    <select id="select" resultType="com.baizhi.liuzz.entity.Distribution">
        select count(*) value,address name from t_client GROUP BY(address)
    </select>

</mapper>